import classes from './NotFound.module.scss';

export type Props = {};

const NotFound = (props:Props) => {
    return (
        <div className={classes["component-style"]}>
            NotFound
        </div>
    );
}

export default NotFound;